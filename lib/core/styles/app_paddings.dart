import 'package:flutter/material.dart';

abstract class AppPaddings {
  static const all20 = EdgeInsets.all(20.0);
  static const all16 = EdgeInsets.all(16.0);
  static const all12 = EdgeInsets.all(12.0);
  static const all14 = EdgeInsets.all(14.0);
  static const all8 = EdgeInsets.all(8.0);
  static const horizontal16 = EdgeInsets.symmetric(horizontal: 16.0);
  static const horizontal20 = EdgeInsets.symmetric(horizontal: 20.0);
  static const vertical16 = EdgeInsets.symmetric(vertical: 16.0);
  static const vertical12 = EdgeInsets.symmetric(vertical: 12.0);
  static const horizontal12 = EdgeInsets.symmetric(horizontal: 12.0);
}
